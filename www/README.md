# WebServ Static Files

This directory contains static files served by the web server:

- `index.html` - Default homepage
- Add your HTML, CSS, JS, and other static assets here

The web server will serve these files directly for GET requests.